services:
  backend:
    build:
      context: ./backend
    container_name: foreman-backend
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: foreman-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "3000:80"
